1. clases
    metodos
    propiedades(atributos)
    encapsulacion
        private   this
        public    this|var
        protected -> public para los herederos y privado para la instancia
2. Herencia, Composicion(x)
3. Avanzados
    Array,List<T>, Extension Method, Delegates, IEnumerable<T> ->Linq
4. Solid
5. Patrones de diseño (Paint)